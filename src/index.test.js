import {expect}     from "chai";
import jsdom        from "jsdom";
import fs           from "fs";

describe("A first test", ()=> {
    it("Should pass", () => {
        expect(true).equals(true);
    });
});